ENTERPRISE_ALL_SERVICE_USERNAMES = ["{{ ENTERPRISE_USER }}"]

ENTERPRISE_SERVICE_WORKER_USERNAME = "{{ ENTERPRISE_USER }}"

# --- Enterprise portal MFEs (learner/admin) runtime config overrides ---

MFE_CONFIG_OVERRIDES = globals().get("MFE_CONFIG_OVERRIDES", {})

MFE_CONFIG_OVERRIDES.setdefault("learner-portal-enterprise", {}).update({
    {% if ENTERPRISECATALOG_HOST is defined and ENTERPRISECATALOG_HOST %}
    "ENTERPRISE_CATALOG_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ENTERPRISECATALOG_HOST }}",
    {% endif %}

    {% if LICENSE_MANAGER_HOST is defined and LICENSE_MANAGER_HOST %}
    "LICENSE_MANAGER_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ LICENSE_MANAGER_HOST }}",
    {% endif %}

    "FEATURE_ENROLL_WITH_ENTERPRISE_OFFERS": "false",
    "FEATURE_ENROLL_WITH_CODES": "false",
    "FEATURE_ENABLE_AUTO_APPLIED_LICENSES": "false",
    "FEATURE_ENABLE_TOP_DOWN_ASSIGNMENT": "false",
    "FEATURE_ENABLE_EMET_REDEMPTION": "false",
})

MFE_CONFIG_OVERRIDES.setdefault("admin-portal", {}).update({
    {% if ECOMMERCE_HOST is defined and ECOMMERCE_HOST %}
    "ECOMMERCE_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ECOMMERCE_HOST }}",
    {% endif %}

    {% if ENTERPRISE_ACCESS_HOST is defined and ENTERPRISE_ACCESS_HOST %}
    "ENTERPRISE_ACCESS_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ENTERPRISE_ACCESS_HOST }}",
    {% endif %}

    {% if ENTERPRISECATALOG_HOST is defined and ENTERPRISECATALOG_HOST %}
    "ENTERPRISE_CATALOG_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ ENTERPRISECATALOG_HOST }}",
    {% endif %}

    {% if LICENSE_MANAGER_HOST is defined and LICENSE_MANAGER_HOST %}
    "LICENSE_MANAGER_BASE_URL": "{% if ENABLE_HTTPS %}https{% else %}http{% endif %}://{{ LICENSE_MANAGER_HOST }}",
    {% endif %}


    "FEATURE_LEARNER_CREDIT_MANAGEMENT": False,
})