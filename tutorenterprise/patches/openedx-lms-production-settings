# Enterprise settings
ENTERPRISE_API_URL = "{{ "https" if ENABLE_HTTPS else "http" }}://{{ LMS_HOST }}/enterprise/api/v1/"
ENTERPRISE_LEARNER_PORTAL_BASE_URL = "{{ 'https' if ENABLE_HTTPS else 'http' }}://apps.{{ LMS_HOST }}/enterprise"
# OAuth2 settings for Enterprise backend service
ENTERPRISE_BACKEND_SERVICE_EDX_OAUTH2_PROVIDER_URL = "http://lms:8000/oauth2"
ENTERPRISE_BACKEND_SERVICE_EDX_OAUTH2_KEY = "{{ ENTERPRISE_BACKEND_SERVICE_EDX_OAUTH2_KEY }}"
ENTERPRISE_BACKEND_SERVICE_EDX_OAUTH2_SECRET = "{{ ENTERPRISE_BACKEND_SERVICE_EDX_OAUTH2_SECRET }}"
# Add Enterprise system-wide roles
SYSTEM_WIDE_ROLE_CLASSES = SYSTEM_WIDE_ROLE_CLASSES + [
    'system_wide_roles.SystemWideRoleAssignment',
    'enterprise.SystemWideEnterpriseUserRoleAssignment',
]